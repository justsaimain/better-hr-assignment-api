scalar DateTime
    @scalar(class: "Nuwave\\Lighthouse\\Schema\\Types\\Scalars\\DateTime")

type User {
    id: String!
    name: String!
    email: String!
    created_at: DateTime!
    updated_at: DateTime!
}

type Employee {
    id: String!
    name: String!
    email: String!
    phone: String!
    user: User!
    created_at: DateTime!
    updated_at: DateTime!
}

type Query {
    employees: [Employee!]! @guard @paginate
    employee(id: ID! @eq): Employee @guard @find
}

type Mutation {
    createEmployee(
        name: String!
        email: String!
        phone: String!
        user_id: Int!
    ): Employee! @guard @create

    updateEmployee(
        id: ID!
        name: String!
        email: String!
        phone: String!
        user_id: Int!
    ): Employee! @guard @update

    deleteEmployee(id: ID!): Employee @guard @delete

    loginUser(email: String!, password: String!): String!

    logoutUser: String! @guard
}
